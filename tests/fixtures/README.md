# tests/fixtures/

Sanitized positive and negative fixtures used to test canonical Sigma rules offline.

## Structure
- one directory per ATT&CK technique
- `positive/` for expected matches
- `negative/` for expected non-matches

## Purpose
Fixture tests prove rule behavior without requiring live lab access. They do not replace live validation.
